defmodule RtcAppWeb.RtcLive.Session do
  def put_user(username, user_id) do
    IO.inspect(user_id, username)
  end

  #def get_user(username, user_id) do
  #  username = username
  #  user_id = user_id
  #end
end
